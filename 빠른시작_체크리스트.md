# 냉장고 파먹기 레벨업 챌린지 — 빠른 시작 체크리스트

> **예상 시간:** 30~45분 · **원칙:** `Run once` 통과 전에는 Make Scheduling을 켜지 않습니다.

## 1. Apps Script 설치 — 10분

- [ ] `https://script.google.com` → **새 프로젝트**
- [ ] 프로젝트 이름: `냉장고 파먹기 레벨업 챌린지`
- [ ] 기본 `Code.gs` 전체 삭제 → `apps_script/Code.gs` 전체 붙여넣기 → 저장
- [ ] **프로젝트 설정(톱니) → 편집기에 appsscript.json 표시** ON
- [ ] `appsscript.json` 열기 → `apps_script/appsscript.json` 전체 붙여넣기 → 저장
- [ ] 함수 선택 → `setupFridgeChallenge` → **실행**
- [ ] **권한 검토 → 계정 선택 → 고급 → 프로젝트로 이동 → 허용**
- [ ] 실행 완료 후 `selfCheckFridgeChallenge` 실행 → `ok=true`

## 2. CONFIG 확인 — 5분

운영 DB `냉장고 파먹기_운영DB` → `CONFIG` 탭:

- [ ] `SPREADSHEET_ID/URL`
- [ ] `JOIN_FORM_EDIT_URL/PUBLISHED_URL`
- [ ] `JOIN_RESPONSE_SHEET_ID` → `참가신청_응답`
- [ ] `MISSION_FORM_EDIT_URL/PUBLISHED_URL`
- [ ] `MISSION_RESPONSE_SHEET_ID` → `미션인증_응답`
- [ ] `CERTIFICATE_TEMPLATE_ID`, `CERTIFICATE_FOLDER_ID`
- [ ] `ADMIN_EMAIL`
- [ ] Make 운영 시 `PROJECT_MODE=MAKE_ASSET_ONLY`, `STANDALONE_ENABLED=N`

## 3. 선택: 사진을 파일 업로드로 교체 — 5분

- [ ] `MISSION_FORM_EDIT_URL` 열기
- [ ] 기존 URL 단답형 `인증 사진` 삭제
- [ ] **질문 추가(+) → 파일 업로드 → 계속**
- [ ] 제목을 정확히 `인증 사진`
- [ ] 이미지 파일만, 1개 이상, **필수** ON
- [ ] 같은 제목 문항이 1개만 있는지 확인
- [ ] `setupFridgeChallenge()` 재실행
- [ ] `미션인증_응답` 헤더와 Make SCN-02 사진 열 재매핑

> 파일 업로드는 Google 로그인·Drive 저장 용량·조직 외부 업로드 제한을 확인하세요.

## 4. Make Blueprint 가져오기 — 15분

각 파일마다 **Scenarios → Create a new scenario → 더보기(⋯) → Import Blueprint**:

1. [ ] `01_signup_welcome.blueprint.json`
2. [ ] `02_mission_levelup.blueprint.json`
3. [ ] `03_reminder.blueprint.json`

`MAPPING_MANIFEST.json`은 참고 파일이며 가져오지 않습니다.

### 모든 시나리오 공통

- [ ] Scheduling OFF
- [ ] 모든 Google Sheets 모듈 → 본인 Google 연결
- [ ] Spreadsheet → `냉장고 파먹기_운영DB` 재선택
- [ ] 모든 Gmail 모듈 → 발신 Gmail 연결
- [ ] 모든 Sheet·열·필터·동적 토큰을 한국어 헤더명으로 재선택
- [ ] `__MISSION_FORM_PREFILL_BASE_URL__` → 실제 Form/사전 작성 URL
- [ ] 빨간 경고 아이콘 없음
- [ ] 시간대 `Asia/Seoul`

### SCN-01

- [ ] 모듈 1 → `참가신청_응답`
- [ ] 모듈 2 → `레벨업트래커`, `이메일주소(E)` 검색
- [ ] **Search Rows 0건이어도 계속 실행** ON
- [ ] 모듈 4 → Add Row 39열 재매핑
- [ ] 모듈 5/7 → Gmail 수신자·본문·링크
- [ ] 모듈 6 → 검색/추가된 Row number와 `AA/AB/AK`

### SCN-02

- [ ] 모듈 1 → `미션인증_응답`
- [ ] 모듈 2 → `레벨업트래커`, `참가자ID(A)` 검색
- [ ] **Search Rows 0건이어도 계속 실행** ON
- [ ] Router 전체 필터 → 이메일·사진·제출레벨·현재레벨·상태 재선택
- [ ] Update Row `23/26/29/32/35` → Row number 및 Lv 상태 열 재매핑
- [ ] Gmail `10~14/24/27/30/33/36/50` 재매핑
- [ ] 순차 처리/동시 실행 수 `1`

### SCN-03

- [ ] 모듈 1 → `레벨업트래커`, `Z≠완료`, 정렬 `AB`, Limit 500
- [ ] 필터 → `AB≤now`, `AC=N`, 현재 Lv 상태 `K/N/Q/T/W=진행중`
- [ ] 모듈 2 → Gmail 수신자·레벨·칭호·링크
- [ ] 모듈 3 → `AC=Y`, `AD=now`, `AE=이벤트`, `AK=성공`
- [ ] 예약: 매일 `09:00 Asia/Seoul` — 아직 OFF

## 5. Run once — 10분

### 참가 신청

- [ ] SCN-01 → **Run once** → 시작 위치 `From now on`
- [ ] 참가 Form에 실제 수신 가능한 테스트 이메일 제출
- [ ] 트래커 1행 + 웰컴 Gmail 확인
- [ ] 같은 이메일 재제출 → 중복 행 없음 + 중복 안내

### 미션 인증

- [ ] SCN-02 → **Run once**
- [ ] Lv.1 사진·재료·요리 이름 제출
- [ ] `Lv1상태=완료`, `현재레벨=2`, `Lv2상태=진행중`
- [ ] Lv.2 Gmail 수신
- [ ] Update Row가 다른 열을 지우지 않았는지 확인

### 리마인드

- [ ] 테스트 행 `AB=현재보다 과거`, `AC=N`
- [ ] SCN-03 → **Run once**
- [ ] Gmail 1회, `AC=Y`, `AD=발송시각`
- [ ] 재실행 → 두 번째 Gmail 없음

## 6. 필수 예외 테스트

- [ ] Lv.1~Lv.5 순차 통과
- [ ] 사진 누락 → 미통과·재제출 안내·레벨 유지
- [ ] Lv.1 진행중에 Lv.3 제출 → 순서 오류·상태 유지
- [ ] 완료한 Lv.1 재제출 → 중복 안내·완료값 유지
- [ ] 5일 리마인드 → 같은 레벨 1회만
- [ ] Lv.5 중복 → 인증서ID·PDF 1개만 유지
- [ ] Make History에서 각 Router 경로 실행 버블 확인

## 7. 인증서 자동 발급 보완

Make SCN-02 Blueprint는 Lv.5 완료까지만 기록합니다.

- [ ] Apps Script → **트리거(시계) → 트리거 추가**
- [ ] 함수 `issuePendingCertificates`
- [ ] 이벤트 소스 **시간 기반** → **시간 타이머** → 매시간
- [ ] 저장·권한 승인
- [ ] 수동 1회 실행 후 `AF=발급완료`, `AG/AH/AI` 확인
- [ ] 두 번째 실행에도 PDF가 추가 생성되지 않음

> `installStandaloneTriggers()`는 Make와 함께 실행하지 마세요. Form 응답이 중복 처리됩니다.

## 8. 운영 시작

- [ ] SCN-01 Scheduling ON
- [ ] SCN-02 Scheduling ON
- [ ] SCN-03 매일 09:00 Scheduling ON
- [ ] 모두 Save
- [ ] 첫 예약 실행을 Make History에서 확인
- [ ] Drive 사진·인증서 공유 범위 확인
- [ ] 개인정보 동의·보유기간·삭제 일정 확인

## 9. 장애 시 즉시 조치

- [ ] 시나리오 OFF
- [ ] Make History → 실패 모듈 Input/Output·오류 코드 저장
- [ ] Google/Gmail 연결 재승인
- [ ] Spreadsheet·Sheet·열·필터 재선택
- [ ] Search Rows 0건 계속 실행 확인
- [ ] Update Row의 Row number 확인
- [ ] Apps Script → Executions에서 오류 줄 확인
- [ ] 긴급 독립 실행: Make 전부 OFF → `installStandaloneTriggers()`
- [ ] 즉시 리마인드 점검이 필요하면 공개 함수 `runFiveDayReminders()` 수동 실행
- [ ] Make 복귀: `removeStandaloneTriggers()` → `STANDALONE_ENABLED=N` → Make ON

## 최종 승인

- [ ] 실제 계정에서 OAuth·Run once 검증 완료
- [ ] Make UI 버전 차이에 따른 모듈 재저장 완료
- [ ] Gmail·Make Ops 한도 확인
- [ ] Make와 Apps Script Form 제출 트리거 동시 실행 아님
- [ ] 테스트 데이터의 이메일·사진 URL을 외부 캡처에서 마스킹
