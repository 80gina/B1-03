# 냉장고 파먹기 레벨업 챌린지 — Discord 빠른 시작 체크리스트

> **중복 실행 금지:** Make 모드와 Apps Script 독립 실행 모드를 동시에 켜지 않습니다.
>
> **활성화 금지:** 모든 `Run once` 테스트를 통과하기 전에는 Make Scheduling을 켜지 않습니다.

## 1. Apps Script 설치

- [ ] `https://script.google.com`에서 새 프로젝트 생성
- [ ] `apps_script/Code.gs` 전체 붙여넣기·저장
- [ ] 프로젝트 설정에서 `appsscript.json` 표시 ON
- [ ] `apps_script/appsscript.json` 전체 붙여넣기·저장
- [ ] `setupFridgeChallenge()` 실행
- [ ] Sheets·Forms·Drive·Slides·외부 HTTP 요청·트리거 관리 OAuth 승인
- [ ] 실행 로그에서 `spreadsheetUrl`, `joinFormUrl`, `missionFormUrl`, `certificateFolderUrl` 확인
- [ ] `selfCheckFridgeChallenge()` 실행 → `ok=true`

## 2. Discord Webhook

- [ ] Discord **채널 설정 → 연동(Integrations) → Webhooks**에서 Webhook 생성
- [ ] Webhook URL을 문서·시트·소스·공개 저장소에 기록하지 않음
- [ ] 바인딩 UI가 있으면 `setDiscordWebhookUrl()` 또는 **🍲 냉파 레벨업 → Discord Webhook 안전 입력** 실행
- [ ] 독립형 프로젝트이면 Script Properties에 `DISCORD_WEBHOOK_URL` 등록 후 `setDiscordWebhookUrl()` 실행
- [ ] `CONFIG!DISCORD_WEBHOOK_CONFIGURED=Y`
- [ ] `testDiscordWebhook()` 실행 → Discord 테스트 Embed 확인
- [ ] 테스트 메시지의 공개 범위가 `챌린지닉네임 + 참가자ID`인지 확인

## 3. CONFIG와 자산

- [ ] `PROJECT_MODE=MAKE_ASSET_ONLY`
- [ ] `STANDALONE_ENABLED=N`
- [ ] `TIMEZONE=Asia/Seoul`
- [ ] `REMINDER_DAYS=5`, `REMINDER_HOUR=9`
- [ ] `SPREADSHEET_ID`
- [ ] `JOIN_FORM_EDIT_URL`, `JOIN_FORM_PUBLISHED_URL`
- [ ] `MISSION_FORM_EDIT_URL`, `MISSION_FORM_PUBLISHED_URL`
- [ ] `JOIN_RESPONSE_SHEET_ID`, `MISSION_RESPONSE_SHEET_ID`
- [ ] `CERTIFICATE_TEMPLATE_ID`, `CERTIFICATE_FOLDER_ID`
- [ ] 시트 7종 존재: `CONFIG`, `참가신청_응답`, `미션인증_응답`, `레벨업트래커`, `미션설정`, `메시지템플릿`, `운영로그`

## 4. 선택: `인증 사진` 파일 업로드 교체

- [ ] `MISSION_FORM_EDIT_URL` 열기
- [ ] 기존 URL 단답형 `인증 사진` 삭제
- [ ] **질문 추가 → 파일 업로드**
- [ ] 제목을 정확히 `인증 사진`
- [ ] 이미지 파일·1개 이상·필수 ON
- [ ] URL 문항과 파일 업로드 문항을 동시에 두지 않음
- [ ] `setupFridgeChallenge()` 재실행
- [ ] `미션인증_응답`에 `인증 사진` 헤더가 한 번만 존재
- [ ] SCN-02의 사진 열·필터 재매핑

> Apps Script는 URL, Google Drive/Docs 링크, 20자 이상의 Drive 파일 ID를 사진 증거로 인정합니다. 파일 업로드 문항은 사전 작성하지 않습니다.

## 5. Make Blueprint Import

각 파일을 별도 시나리오로 가져옵니다.

- [ ] `01_signup_welcome.blueprint.json`
- [ ] `02_mission_levelup.blueprint.json`
- [ ] `03_reminder.blueprint.json`
- [ ] `MAPPING_MANIFEST.json`은 Import하지 않음
- [ ] 세 시나리오 Scheduling OFF
- [ ] 세 시나리오 순차 실행 ON
- [ ] 세 시나리오 동시 실행 수 `1`
- [ ] 세 시나리오 시간대 `Asia/Seoul`

## 6. Make 공통 재매핑

- [ ] 모든 Google Sheets 모듈에서 운영 DB 계정 OAuth 승인
- [ ] Google 연결 `__IMTCONN__=0`을 실제 연결로 교체
- [ ] `/__SPREADSHEET_ID_FROM_CONFIG__` → `냉장고 파먹기_운영DB`
- [ ] `__SHEET_ID_JOIN_RESPONSE__` → `참가신청_응답`
- [ ] `__SHEET_ID_MISSION_RESPONSE__` → `미션인증_응답`
- [ ] `__SHEET_ID_TRACKER__` → `레벨업트래커`
- [ ] 모든 `__ROW_NUMBER__` → 실제 Row number 출력 토큰
- [ ] `__MISSION_FORM_PREFILL_BASE_URL__` → 실제 미션 Form/사전 작성 URL
- [ ] 파일 업로드 문항은 사전 작성 링크에 넣지 않음
- [ ] `__DISCORD_WEBHOOK_URL__` → Make의 안전한 비밀값 매핑
- [ ] Blueprint 재내보내기 전 실제 Webhook URL 유출 검사
- [ ] 모든 Sheet·열·필터를 한국어 헤더 기준으로 재선택
- [ ] `운영로그`에 JOIN/PASS/REMIND 멱등키를 **Discord 전송 전에 원자 선점**하고 처리중·성공 키를 차단하는 모듈 추가
- [ ] Discord 성공 후 멱등키 `성공`, 최종 실패 후 재시도 가능 실패 상태 기록
- [ ] 모든 Discord HTTP 모듈에 429 `retry_after`/`Retry-After`, 5xx 지수형 최대 3회 Error Handler 추가
- [ ] 위 멱등성·재시도 Make History 증거 없이는 Scheduling OFF 유지
- [ ] 모든 빨간 경고 아이콘 제거

## 7. SCN-01

- [ ] Watch Rows → `참가신청_응답`
- [ ] Search Rows → `레벨업트래커`, `이메일주소` 중복 검색
- [ ] Search Rows 0건이어도 계속 실행 ON
- [ ] Add Row 39열 재매핑
- [ ] 웰컴·중복 Discord HTTP 모듈 URL 확인
- [ ] 모든 payload에 `allowed_mentions.parse=[]`
- [ ] Discord 필드에는 `챌린지닉네임`, `참가자ID`만 참가자 식별값으로 사용

## 8. SCN-02

- [ ] Watch Rows → `미션인증_응답`
- [ ] Search Rows → `레벨업트래커`, `참가자ID` 검색
- [ ] Search Rows 0건이어도 계속 실행 ON
- [ ] 참가자 미확인 필터
- [ ] 이메일 불일치 필터
- [ ] 사진 누락 필터
- [ ] 레벨 순서 오류 필터
- [ ] 완료 레벨 중복 필터
- [ ] Lv.1~Lv.5 통과 필터
- [ ] 레벨별 필수 설명 누락 필터
- [ ] Update Row가 다른 열을 비우지 않는지 확인
- [ ] Lv.5 통과 시 `Lv5상태=완료`
- [ ] Lv.5 통과 시 `전체완료여부=완료`
- [ ] Lv.5 통과 시 `인증서발급상태=미발급`
- [ ] **SCN-02가 인증서 PDF를 즉시 다운로드·첨부하지 않음을 확인**

## 9. SCN-03

- [ ] Search Rows → `레벨업트래커`
- [ ] 완료자가 아닌 행만 대상
- [ ] `다음리마인드예정일시≤now`
- [ ] `현재레벨리마인드발송여부=N`
- [ ] 현재 레벨 상태가 `진행중`
- [ ] 성공 시 `현재레벨리마인드발송여부=Y`
- [ ] 성공 시 `Discord최근발송상태=성공`
- [ ] 예약 `매일 09:00 Asia/Seoul` 설정, 아직 OFF

## 10. `Run once`

### Webhook

- [ ] `testDiscordWebhook()` 성공
- [ ] Discord 채널에 실제 메시지 표시
- [ ] 원치 않는 멘션 없음

### 참가 신청

- [ ] SCN-01 → `Run once` → `From now on`
- [ ] 참가 Form 제출
- [ ] 트래커 1행 생성
- [ ] Discord 웰컴 확인
- [ ] 같은 이메일 재제출
- [ ] 중복 행 없음 + 중복 안내 확인

### 미션 인증

- [ ] SCN-02 → `Run once`
- [ ] Lv.1 사진·재료 3가지 이상·요리 이름 제출
- [ ] `Lv1상태=완료`, `현재레벨=2`, `Lv2상태=진행중`
- [ ] Discord Lv.2 상태창 확인
- [ ] Lv.1~Lv.5 순차 통과
- [ ] 사진 누락 → 미통과·레벨 유지
- [ ] 필수 설명 누락 → 미통과·레벨 유지
- [ ] 현재 Lv.1인데 Lv.3 제출 → 순서 오류·상태 유지
- [ ] 완료 Lv.1 재제출 → 중복 안내·완료값 유지
- [ ] 참가자ID 미확인·이메일 불일치 경로 확인

### 리마인드

- [ ] 테스트 행의 예정일을 과거로, 발송 여부를 `N`으로 설정
- [ ] SCN-03 → `Run once`
- [ ] Discord 리마인드 1회
- [ ] 발송 여부 `Y`
- [ ] 다시 실행해 같은 레벨 두 번째 메시지 없음

## 11. Make 모드 인증서 트리거

- [ ] Apps Script **트리거 → 트리거 추가**
- [ ] 함수 `issuePendingCertificates`
- [ ] 이벤트 소스 **시간 기반**
- [ ] 시간 타이머 또는 운영량에 맞는 주기
- [ ] 저장·권한 승인
- [ ] `installStandaloneTriggers()`는 실행하지 않음
- [ ] Lv.5 완료 테스트 행의 `AF=미발급`
- [ ] `issuePendingCertificates()` 수동 실행
- [ ] `AF=발급완료`
- [ ] `AG=인증서ID`, `AH=PDF URL`, `AI=발급일시`
- [ ] Discord Embed + PDF 직접 첨부 확인
- [ ] 20 MiB 초과/첨부 실패 시 Drive 링크 fallback 확인
- [ ] 다시 실행해 인증서 PDF 중복 없음
- [ ] Discord 공유 최종 실패 시 `AF=발급실패`

## 12. 개인정보·보안

- [ ] Discord 참가자 식별 필드: `챌린지닉네임`, `참가자ID`만
- [ ] Discord에 `이름` 없음
- [ ] Discord에 `이메일주소` 없음
- [ ] Discord에 `휴대전화번호` 없음
- [ ] Discord에 `카카오수신동의` 없음
- [ ] Discord에 인증 사진 URL·Drive 파일 ID 없음
- [ ] 모든 payload에 `allowed_mentions.parse=[]`
- [ ] Webhook URL은 CONFIG·로그·소스·문서에 없음
- [ ] Make History·실행 화면·외부 캡처에서 개인정보와 Webhook URL 마스킹
- [ ] Drive 사진·인증서 공유 범위 최소화
- [ ] 운영 종료 후 응답·사진·로그·Make 실행 데이터 삭제 일정 확정

## 13. 운영 모드 최종 선택

### Make 모드

- [ ] `PROJECT_MODE=MAKE_ASSET_ONLY`
- [ ] `STANDALONE_ENABLED=N`
- [ ] Apps Script Form 제출 독립 트리거 없음
- [ ] `issuePendingCertificates` 시간 트리거만 설치
- [ ] 모든 `Run once` 통과
- [ ] SCN-01 Scheduling ON
- [ ] SCN-02 Scheduling ON
- [ ] SCN-03 매일 09:00 Scheduling ON

### Apps Script 독립 실행 모드

- [ ] SCN-01~03 Scheduling 모두 OFF
- [ ] `CONFIG!MAKE_SCENARIOS_CONFIRMED_OFF=Y` 설정
- [ ] `installStandaloneTriggers()` 실행(확인값이 없으면 코드가 차단하는지 확인)
- [ ] `PROJECT_MODE=APPS_SCRIPT_STANDALONE`
- [ ] `STANDALONE_ENABLED=Y`
- [ ] `handleSpreadsheetFormSubmit` 트리거 존재
- [ ] `runFiveDayReminders` 트리거 존재
- [ ] 참가 신청·미션·리마인드·인증서 테스트 통과

## 14. 장애 시 즉시 조치

- [ ] Make 장애: SCN-01~03 Scheduling OFF
- [ ] History에서 실패 모듈·HTTP 코드·실행 ID 확인
- [ ] Google OAuth 재승인
- [ ] Spreadsheet·Sheet·열·필터·Row number 재선택
- [ ] 429는 `retry_after`, 5xx는 지수형 재시도
- [ ] Webhook 유출/401·403·404 → 기존 Webhook 삭제·새 URL 회전
- [ ] Apps Script 오류 → Executions에서 줄 번호 확인
- [ ] 인증서 `처리중` 장기 지속 → 접근 권한 확인 후 `발급실패`로 복구하고 재실행
- [ ] 긴급 독립 실행 전 Make 전부 OFF
- [ ] Make 복귀 전 `removeStandaloneTriggers()` 실행

## 최종 승인

- [ ] 실제 계정에서 OAuth 완료
- [ ] 모든 `Run once` 테스트 완료
- [ ] 인증서 수동 테스트 완료
- [ ] 개인정보 공개 제한 확인
- [ ] Make와 Apps Script Form 제출 트리거 동시 실행 아님
- [ ] 운영자 승인 후에만 Scheduling ON
